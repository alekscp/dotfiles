alias opencode='export GOOGLE_APPLICATION_CREDENTIALS=$(echo $OPENCODE_GOOGLE_APPLICATION_CREDENTIALS) && export GOOGLE_CLOUD_PROJECT=$(echo $OPENCODE_GOOGLE_CLOUD_PROJECT) && VERTEX_LOCATION=$(echo $OPENCODE_VERTEX_LOCATION) && opencode'
